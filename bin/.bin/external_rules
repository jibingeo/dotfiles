#! /bin/sh

wid=$1
class=$2
instance=$3

if [[ "$class" = Chromium && "$instance" != chromium ]] ; then
	role=$(xprop -id $wid WM_WINDOW_ROLE | cut -d'=' -f2 | tr  -d ' "')
	case "$role" in
		"pop-up")
			echo "focus=off state=floating"
			;;
	esac
fi
